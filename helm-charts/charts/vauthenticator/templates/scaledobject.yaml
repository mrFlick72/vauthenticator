{{- if eq .Values.keda.enabled true }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "vauthenticator.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    deploymentName: {{ include "vauthenticator.name" . }}
spec:
  minReplicaCount: {{ .Values.keda.spec.minReplicaCount }}
  maxReplicaCount: {{ .Values.keda.spec.maxReplicaCount }}
  pollingInterval: {{ .Values.keda.spec.pollingInterval }}
  cooldownPeriod: {{ .Values.keda.spec.cooldownPeriod }}
  scaleTargetRef:
    name: {{ include "vauthenticator.name" . }}
  triggers:
    - type: prometheus
      metadata:
        serverAddress: {{ .Values.keda.prometheus.serverAddress }}
        metricName: {{ .Values.keda.prometheus.metricName }}
        threshold: {{ .Values.keda.prometheus.threshold | quote }}
        query: {{ .Values.keda.prometheus.query }}
{{- end }}
