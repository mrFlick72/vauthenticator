management-ui is deployed as a ClusterIP service reachable at:

  {{ include "management-ui.name" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

{{- if .Values.ingress.enabled }}

An Ingress is also rendered at host {{ .Values.ingress.host }} (path /) for external access.
{{- end }}
{{- if .Values.httpRoute.enabled }}

A Gateway API HTTPRoute is also rendered for external access
{{- if .Values.gateway.enabled }}, bound to a Gateway resource also rendered by this chart{{- end }}.
{{- end }}

nginx proxies GET /api/config to Values.application.configManagerUpstream at request time.
Make sure that value points at the config-manager Service backing this release (see the
config-manager chart's NOTES.txt for its reachable address).
